query getRunConfig($id: ID!) {
  getRunConfig(id: $id) {
    totalTime
    steps {
      time
      description
      deviceName
      timeFrameOptionType
      deviceOption {
        optionName
        deviceOptionType
        options
        selected
        options
      }
    }
  }
}
