query getRunConfigs {
  getRunConfigs {
    runConfigs {
      id
      name
      lastLoaded
      totalTime
      runConfigStatus {
        status
        messages
      }
    }
    canCreateNewRun
  }
}
