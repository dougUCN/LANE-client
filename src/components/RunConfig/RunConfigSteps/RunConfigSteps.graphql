query getRunConfig($id: ID!) {
  getRunConfig(id: $id) {
    totalTime
    steps {
      id
      time
      description
      deviceName
      deviceOptions {
        optionName
        deviceOptionType
        options
        selected
        options
      }
    }
  }
}
