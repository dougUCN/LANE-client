query getRunConfig($id: ID!) {
  getRunConfig(id: $id) {
    totalTime
    steps {
      id
      time
      description
      deviceName
      deviceOption {
        optionName
        deviceOptionType
        options
        selected
        options
      }
    }
  }
}
