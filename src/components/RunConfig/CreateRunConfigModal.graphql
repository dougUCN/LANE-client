mutation createRunConfig($name: String!, $totalTime: Float!) {
  createRunConfig(runConfig: { name: $name, totalTime: $totalTime }) {
    message
    success
    modifiedRunConfig {
      id
    }
  }
}
